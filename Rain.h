#ifndef RAIN_H
#define RAIN_H
#include "Object.h"
#include <stdlib.h>
#include <ctime>

class Rain:public Object
{
public:
   // static Rain GenerateUL();
   //static Rain GenerateBR(int x, int y);
   Rain();

private:
// Rain(int a, int b); //only the program can create objects of type Rain

};

#endif // RAIN_H
